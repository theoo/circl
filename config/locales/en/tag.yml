---
en:
  tag:
    errors:
      cannot_be_its_own_parent: cannot be its own parent
      cannot_destroy_tag_if_it_has_members: cannot destroy tag if it has members
    notices:
      members_added: members added
    views:
      actions:
        add_members: add members
        remove_all_members: remove all members
        view_tag_members: view tag members
      add_members_message: add members message
      add_members_title: add members title
      members: members
