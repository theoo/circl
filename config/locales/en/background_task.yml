---
en:
  background_task:
    mail:
      error_report_title: error report title
    tasks:
      add_people_to_subscription_and_email: Adding %{people_count} people to subscription %{subscription_id} - %{subscription_title}
      concat_and_email_subscription_pdf: Merging in a file invoices from %{people_count} people of subscription %{subscription_id} - %{subscription_title}
      generate_invoice_pdf: Generating PDF for invoice %{invoice_id} - %{invoice_title}
      generate_invoice_template_jpg: Generating JPG for invoice template %{invoice_template_id} - %{invoice_template_title}
      generate_salary_pdf: Generating PDF for salary %{salary_id} - %{salary_title}
      generate_salary_template_jpg: Generating JPG for salary template %{salary_template_id} - %{salary_template_title}
      merge_subscriptions: Merging subscription %{source_subscription_id} in %{destination_subscription_id}
      prepare_subscription_pdf_and_email: Generating PDF for %{people_count} people invoices, concerning %{subscription_id} - %{subscription_title}
      run_rake_task: Executing extraordinary task - %{name}
      update_index_for_people: Updating search engine index for %{people_count} people
      update_subscription_invoices_and_email: Updating indices and invoices for subscription %{subscription_id} - %{subscription_title}
    views:
      actions:
        destroy_background_task: destroy the task
      background_tasks_list_title: background tasks list title
      status: status
      statuses:
        pending: pending
        running: running
      tasks_pending: background tasks
