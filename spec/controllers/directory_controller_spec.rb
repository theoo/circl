require "spec_helper"

describe DirectoryController do

  describe "search engine" do
    it "should return all people when there is no query string" do
      pending
    end

    it "should redirect to person edit when query return only one person" do
      pending
    end

    it "should probably have more tests" do
      pending
    end
  end


end
